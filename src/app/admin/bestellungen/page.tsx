import { Metadata } from 'next';

export const metadata = {
    title: '',
    description: '',
};

export default function OrderManagementPage() {
    return <section>Es gibt noch keine Bestellungen</section>;
}
