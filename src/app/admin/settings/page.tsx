// import { Metadata } from 'next';

export const metadata = {
    title: '',
    description: '',
};

export default function AdminSettingsPage() {
    return <section>Hier kommen die Einstellungen</section>;
}
