// import { Metadata } from 'next';

export const metadata = {
    title: '',
    description: '',
};

export default function CustomerManagementPage() {
    return <section>Die Kunden kommen noch.</section>;
}
